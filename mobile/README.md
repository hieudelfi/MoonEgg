# mobile/ — Flutter (giai đoạn 3)

Chưa dựng. Flutter và Dart **chưa cài trên máy này** (kiểm ngày 2026-09-24: `command -v flutter`
không thấy gì), nên P.1 không chạy được `flutter create`.

Task dựng thư mục này: **P.3** cài môi trường, **3.1** port lõi FSRS sang Dart vào
`mobile/core_dart/`, **3.2** lưu trữ drift, **3.6** tám màn hình.

Job `mobile` trong `.github/workflows/ci.yml` đã viết sẵn và tự bỏ qua khi chưa có
`mobile/pubspec.yaml`. Cài Flutter xong thì job tự chạy, không phải sửa workflow.
