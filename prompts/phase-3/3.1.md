# 3.1 — Port lõi sang Dart

**Tham chiếu:** Kế hoạch §6 · Kiến trúc §2, §8.4 · golden Dart

## Mục tiêu
Xem dòng task 3.1 trong `docs/09-ke-hoach.md` (cột "Làm gì"). Đọc các mục tài liệu ở "Tham chiếu" trước khi bắt đầu.

## Cách làm
1. Chép cột "Checklist" của task 3.1 từ `docs/09-ke-hoach.md` vào `records/3.1.md`, mỗi ☐ thành một dòng `- [ ]`.
2. Đối chiếu từng bước với mô tả behavior/kiến trúc ở mục tham chiếu; nếu tài liệu và mockup lệch nhau, ưu tiên `docs/01-yeu-cau.md`, ghi lệch vào Câu hỏi mở.
3. Làm từng bước; sau mỗi bước ghi "kết quả:" vào bản ghi và commit `3.1: <bước>`.

## Kiểm tra (test đối chiếu)
Chạy các TC ghi ở "Tham chiếu" theo `docs/08-kiem-thu.md`; ghi Input/Output thật vào bảng Kiểm tra. Test vàng: `npm test` trong web/ hoặc `flutter test` trong mobile/ phải 100%.

## Xác minh output trước khi đóng
Theo `CLAUDE.md` §3 cho loại đầu ra của task (dữ liệu / mã / màn hình / gói). Chưa qua → không đóng.

## Bằng chứng cần nộp
Theo cột "Bằng chứng kết quả" của task 3.1 trong `docs/09-ke-hoach.md`.
